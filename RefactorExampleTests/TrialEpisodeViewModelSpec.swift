import Quick
import Nimble

class TrialEpisodeViewModelSpec : QuickSpec {
    override func spec() {
        describe("TrialEpisodeViewModel") {
            it("hey") {
                expect(1).to(equal(1))
            }
        }
    }
}
